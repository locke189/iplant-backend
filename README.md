# iplant-backend (0.1.0)
iPlant - Backend Server

Prerequisites

Mosquitto powered MQTT broker
-osx: brew install mosquitto
-linux: sudo apt-get install mosquitto

paho-mqtt python broker
sudo pip install paho-mqtt
